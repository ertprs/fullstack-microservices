apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  local:
    push: false
artifacts:
  - image: kevinmitaki/fms-auth
    context: auth
    docker:
      dockerfile: Docker
    sync:
      manual:
        - src: "src/**/*.ts"
          dest: .
